# App Framework Note

Apps can be built with any UI framework as long as they compile to static assets
and expose a mount entry point (e.g., `window.NeiaMyAppMount`).

Future work:
- Provide a React-based app template option.
- Document React build + mount pattern in the app scaffold guide.
