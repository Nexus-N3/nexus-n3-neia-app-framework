# App Template TODO

We need a clean developer workflow for creating and publishing new apps.

Planned improvements:
- Create a dedicated app template (Vite + step wizard + gateway API wiring).
- Provide a standalone repo or `templates/` folder that developers can clone.
- Document the workflow: clone -> dev server -> build -> publish -> install.
- Optionally add a `neia` CLI to scaffold/build/install apps.
- Ensure apps can be developed independently of the NEIA dashboard UI.
